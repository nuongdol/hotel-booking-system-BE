payment:
  vnpay:
    tmn-code:
    secret-key:
    init-payment-url:
    return-url:
    timeout:

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

spring:
  profiles:
    active: prod

open:
  api:
    title: API document
    version: v1.0.0.0
    description: API booking hotel system
    serverUrl: http://localhost:8081
    serverName: server test
